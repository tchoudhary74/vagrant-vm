{
  "info": {
    "name": "Endpoint Testing Collection",
    "_postman_id": "unique-id-for-collection",
    "description": "Collection to test UI and Apigee endpoints",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "item": [
    {
      "name": "Direct Env - IPL Business Dates",
      "request": {
        "url": "{{baseUrl}}/api/risk/ipl/v1/businessDatesForContext/IPLRESULTS",
        "method": "GET",
        "header": [
          {
            "key": "origin",
            "value": "",
            "type": "text"
          }
        ]
      },
      "event": [
        {
          "listen": "test",
          "script": {
            "exec": [
              "pm.test(\"Status is success\", function () {",
              "    pm.expect(pm.response.code).to.be.oneOf([200, 401, 500, 503]);",
              "});",
              "",
              "if (pm.response.code === 200) {",
              "    console.log(\"working fine\");",
              "} else if (pm.response.code === 401) {",
              "    console.log(\"working but unauthorized\");",
              "} else if (pm.response.code === 500) {",
              "    console.log(\"server down\");",
              "} else if (pm.response.code === 503) {",
              "    console.log(\"server doesn’t process the endpoint\");",
              "} else {",
              "    console.log(\"unknown error, maybe CORS error\");",
              "}"
            ],
            "type": "text/javascript"
          }
        }
      ]
    },
    {
      "name": "Direct Env - Risk Data Management Count",
      "request": {
        "url": "{{baseUrl}}/api/risk/risk-data-mgmt/v1/risk-code-category-value/actions/count",
        "method": "GET",
        "header": [
          {
            "key": "origin",
            "value": "",
            "type": "text"
          }
        ]
      },
      "event": [
        {
          "listen": "test",
          "script": {
            "exec": [
              "pm.test(\"Status is success\", function () {",
              "    pm.expect(pm.response.code).to.be.oneOf([200, 401, 500, 503]);",
              "});",
              "",
              "if (pm.response.code === 200) {",
              "    console.log(\"working fine\");",
              "} else if (pm.response.code === 401) {",
              "    console.log(\"working but unauthorized\");",
              "} else if (pm.response.code === 500) {",
              "    console.log(\"server down\");",
              "} else if (pm.response.code === 503) {",
              "    console.log(\"server doesn’t process the endpoint\");",
              "} else {",
              "    console.log(\"unknown error, maybe CORS error\");",
              "}"
            ],
            "type": "text/javascript"
          }
        }
      ]
    },
    {
      "name": "Apigee - Risk Data Management Metadata",
      "request": {
        "url": "{{apigeeUrl}}/api/risk/risk-data-mgmt/api/metadata/entities",
        "method": "GET",
        "header": [
          {
            "key": "origin",
            "value": "",
            "type": "text"
          },
          {
            "key": "target",
            "value": "dev-{{env}}",
            "type": "text"
          }
        ]
      },
      "event": [
        {
          "listen": "test",
          "script": {
            "exec": [
              "pm.test(\"Status is success\", function () {",
              "    pm.expect(pm.response.code).to.be.oneOf([200, 401, 500, 503]);",
              "});",
              "",
              "if (pm.response.code === 200) {",
              "    console.log(\"working fine\");",
              "} else if (pm.response.code === 401) {",
              "    console.log(\"working but unauthorized\");",
              "} else if (pm.response.code === 500) {",
              "    console.log(\"server down\");",
              "} else if (pm.response.code === 503) {",
              "    console.log(\"server doesn’t process the endpoint\");",
              "} else {",
              "    console.log(\"unknown error, maybe CORS error\");",
              "}"
            ],
            "type": "text/javascript"
          }
        }
      ]
    },
    {
      "name": "Direct Env - OFRA Exchange Complex",
      "request": {
        "url": "{{baseUrl}}/api/risk/e-ofra/v1/exchangeComplex",
        "method": "GET",
        "header": [
          {
            "key": "origin",
            "value": "",
            "type": "text"
          }
        ]
      },
      "event": [
        {
          "listen": "test",
          "script": {
            "exec": [
              "pm.test(\"Status is success\", function () {",
              "    pm.expect(pm.response.code).to.be.oneOf([200, 401, 500, 503]);",
              "});",
              "",
              "if (pm.response.code === 200) {",
              "    console.log(\"working fine\");",
              "} else if (pm.response.code === 401) {",
              "    console.log(\"working but unauthorized\");",
              "} else if (pm.response.code === 500) {",
              "    console.log(\"server down\");",
              "} else if (pm.response.code === 503) {",
              "    console.log(\"server doesn’t process the endpoint\");",
              "} else {",
              "    console.log(\"unknown error, maybe CORS error\");",
              "}"
            ],
            "type": "text/javascript"
          }
        }
      ]
    }
  ]
}
